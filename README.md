Run npm run deploy in project folder to create a build. One of the steps (package.json) on deploy is to deploy the build to this github repo which will serve the build.

https://ninastoecklein.github.io/
